function logintogglePassword() {
    var togglePass = document.getElementById("loginPassword");
    if (togglePass.type === "password") {
      togglePass.type = "text";
    } else {
      togglePass.type = "password";
    }
  }


  function registogglePassword() {
    var togglePass = document.getElementById("regisPass");
    if (togglePass.type === "password") {
      togglePass.type = "text";
    } else {
      togglePass.type = "password";
    }
  }

  function retogglePassword() {
    var togglePass = document.getElementById("regisRepass");
    if (togglePass.type === "password") {
      togglePass.type = "text";
    } else {
      togglePass.type = "password";
    }
  }